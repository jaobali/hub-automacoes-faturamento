services:
  database:
    image: "postgres:16.0-alpine3.18"

    environment:
      POSTGRES_PASSWORD: "local_password"
      POSTGRES_USER: "postgres"
      POSTGRES_DB: "hub"
    
    ports:
      - "5432:5432"